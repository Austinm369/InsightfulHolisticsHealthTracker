/**
 * Application-wide configuration
 */
export const AppConfig = {
  // Server configuration
  server: {
    port: 5173,
    open: true
  },

  // Build configuration  
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
};